FROM node:22-alpine

WORKDIR /app

# Install dependencies first for better caching
COPY package*.json ./
RUN npm install

# Copy the rest of the application
COPY . .

# Expose Vite's default port
EXPOSE 5173

# Start Vite in dev mode, binding to all interfaces so Docker can expose it
CMD ["npm", "run", "dev", "--", "--host"]
